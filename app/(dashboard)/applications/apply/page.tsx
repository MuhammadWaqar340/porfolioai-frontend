import { ApplyJobWizard } from "@/components/applications/apply-job-wizard";

export const metadata = {
  title: "Apply to job",
};

export default function ApplyToJobPage() {
  return <ApplyJobWizard />;
}
